# Workspace Directory

This directory contains cloned external projects that Developer Factory manages.

Each subdirectory is a separate git repository.
Do not manually edit files here - let the automation handle it.
